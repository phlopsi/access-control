sudo: false
language: php

php:
    - nightly

before_script:
    - composer install

script:
    - ./vendor/bin/phpcs --colors --report-width=auto --warning-severity=0 --standard=PSR2 src
    - ./vendor/bin/phpunit